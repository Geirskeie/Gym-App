import { useEffect, useState } from "react";
import { View, Text, FlatList } from "react-native";

const ApiNinja = ({ muscle }) => {
  return (
    <View>
      <Text>{data[1].name}</Text>
    </View>
  );
};
export default ApiNinja;
